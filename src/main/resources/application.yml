spring:
  application:
    name: resume-builder

server:
  port: 8080

logging:
  level:
    root: INFO
    service: DEBUG

openai:
  api-key: OPENAI_API_KEY_PLACEHOLDER
  url: https://api.openai.com/v1/chat/completions
  model: gpt-4o-mini